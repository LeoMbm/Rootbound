# Codexless V5 — Local Mac Release Validation

Date: 2026-08-19
Branch: `feat/thread-history-continuity`
Validated commit: `ae3897da785a18f917f848f8b1c83fbbdc1bb7c8`
Environment observed during validation: macOS, Node.js 24.10.0

## Result

Local trusted-machine release validation passed **for the V5 core at the validated commit above**.

Verified successfully:

- `npm run check:lock:strict`
- `npm ci`
- `npm run validate:v5`
- syntax validation
- full V5 test suite
- `npm test`
- `npm pack --dry-run`
- `npm run validate:release`

The canonical npm lock is `npm-shrinkwrap.json`.

## Important scope note

After this green run, V5 gained the guided one-command `codexless connect .` onboarding flow. That later work changes tunnel discovery/configuration, private runtime-key handling, tunnel validation, and the first-run UX.

Therefore this document is **historical evidence for the core V5 lane, not validation evidence for the newer guided-connect implementation**. The post-wizard branch must rerun `npm run validate:v5` / `npm run validate:release` and complete a clean guided-connect acceptance before merge.

## Bugs found and fixed during local validation

The first full Mac pass exposed several path-identity bugs caused by macOS aliases such as `/var/...` resolving to `/private/var/...`. These were fixed in project refs, exact-root workspace trust, undo/redo mutation paths, and construction-tool scope checks.

The validation flow was also hardened so syntax validation runs before the V5 test suite.

Real-machine checks performed after the core suite also demonstrated exact-root trust, `doctor` status `ok`, model-free `self-test` read/command/write cleanup, project registry persistence, and a supervised tunnel runtime reaching `running`.

## Remaining gates

Still required after the guided-connect changes:

- rerun the trusted-machine V5/release suites;
- clear the prior manual tunnel setup and validate plain `codexless connect .` end-to-end;
- verify runtime-key secrecy / local file permissions and automatic tunnel doctor;
- current ChatGPT connector acceptance;
- one controlled macOS + Windows validation run before supported release;
- real-machine Windows acceptance including ACL hardening and buffered long-command fallback;
- final upgrade / uninstall acceptance on both supported platforms;
- final release hygiene / branch review.

The PR remains draft until those gates are complete.
