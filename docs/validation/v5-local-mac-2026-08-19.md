# Codexless V5 — Local Mac Release Validation

Date: 2026-08-19
Branch: `feat/thread-history-continuity`
Validated commit: `ae3897da785a18f917f848f8b1c83fbbdc1bb7c8`
Environment observed during validation: macOS, Node.js 24.10.0

## Result

Local trusted-machine release validation passed.

Verified successfully:

- `npm run check:lock:strict`
- `npm ci`
- `npm run validate:v5`
- syntax validation
- full V5 test suite
- `npm test`
- `npm pack --dry-run`
- `npm run validate:release`

The canonical npm lock is `npm-shrinkwrap.json`.

## Bugs found and fixed during local validation

The first full Mac pass exposed several path-identity bugs caused by macOS aliases such as `/var/...` resolving to `/private/var/...`. These were fixed in project refs, exact-root workspace trust, undo/redo mutation paths, and construction-tool scope checks.

The validation flow was also hardened so syntax validation runs before the V5 test suite.

## Remaining gates

This local green run does not replace the supported-runtime / release acceptance gates. Still required before merge:

- one controlled manual GitHub Actions matrix on macOS + Windows using the target Node 22.13+/22.16 runtimes;
- real-machine Mac acceptance for install, tunnel, connect, workspace/read/edit, long commands, continuity, diagnostics, upgrade, and uninstall;
- real-machine Windows acceptance including buffered long-command fallback and unsupported stdin behavior;
- current ChatGPT connection acceptance;
- final release hygiene / branch review.

The PR remains draft until those gates are complete.
