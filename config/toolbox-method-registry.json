{
  "schemaVersion": 1,
  "verifiedCodexVersion": "0.148.0-alpha.15",
  "additionalAcceptedCodexVersions": ["0.148.0-alpha.9"],
  "verifiedAt": "2026-08-18",
  "defaultAction": "deny",
  "remoteAllowlist": {
    "command/exec": {
      "classification": "model-free",
      "bridgeTools": [
        "codex.command_exec",
        "codex.command_start",
        "codex.command_poll",
        "codex.repo_search",
        "codex.read_many",
        "codex.apply_patch",
        "codex.precise_edit",
        "codex.edit_undo",
        "codex.edit_redo",
        "codex.git_status",
        "codex.git_diff"
      ],
      "evidence": [
        "Codex 0.148.0-alpha.15 App Server documentation describes command/exec as running one command without starting a thread/turn.",
        "The accepted App Server implementation supports processId plus streamed stdout/stderr deltas on the same connection.",
        "Codexless rejects model-turn/token-usage notifications observed during model-free command execution."
      ]
    },
    "command/exec/write": {
      "classification": "model-free",
      "bridgeTools": ["codex.command_write"],
      "evidence": [
        "The accepted App Server command streaming contract writes stdin to an existing processId and does not start a model turn."
      ]
    },
    "command/exec/terminate": {
      "classification": "model-free",
      "bridgeTools": ["codex.command_terminate"],
      "evidence": [
        "The accepted App Server command streaming contract terminates an existing processId and does not start a model turn."
      ]
    }
  },
  "internalHostAllowlist": {},
  "denyFamilies": [
    {
      "pattern": "turn/*",
      "classification": "model-execution-surface",
      "reason": "ChatGPT-only public surface does not expose any Codex model turn or agent delegation path."
    },
    {
      "pattern": "review/*",
      "classification": "model-execution-surface",
      "reason": "ChatGPT-only public surface does not expose Codex model execution."
    },
    {
      "pattern": "model/*",
      "classification": "model-routing-surface",
      "reason": "Model catalog/routing is intentionally absent so ChatGPT cannot select a Codex fallback model."
    },
    {
      "pattern": "account/*",
      "classification": "codex-account-surface",
      "reason": "Codex quota/account state is intentionally absent from the ChatGPT-only public surface."
    }
  ],
  "observeOnly": {}
}
